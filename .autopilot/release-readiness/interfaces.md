# Границы и швы

<!-- Скопировано из spec.md §Границы и швы. Phase 5 читает этот файл перед каждым таском. -->

## Проектные правила (не выводить — брать как есть)

- 0 кредитов: никаких списаний creatify/scrapecreators. Допустимы: static analysis, мок-симуляции, бесплатные live-проверки (активация, executions, TG).
- TG-ноды: typeVersion telegram v1.2, switch v3.4; neverError — вложенный `options.response.response.neverError`; esc(); callback_data `={{ }}`; HTTP-ноды платных вызовов — typeVersion 4.5 + keypair-заголовки.
- Секреты — только имена переменных; значения в `.env` на сервере.
- Источник правды воркфлоу: сервер `~/factory/`; репо `workflows/` — синк 16.08; `.scratch/*/fixes/` — история, есть ловушки.
- Приёмка: Robotec (client_id=1), платформы IG/TikTok/YouTube/TG/X/Threads/VK, P2 вне scope, постмypost-аккаунты — действие пользователя.

## Модули

| Модуль | Владеет | Выставляет | Прячет |
|---|---|---|---|
| аудит-кластер | свои воркфлоу/файлы | отчёт находок формата `{id, кластер, severity, файл:нода, доказательство, гипотеза фикса, платный_риск}` | метод поиска |
| фикс-таск | один файл/одна цепочка | готовый JSON/файл + отчёт валидации (0 issues, sim) | промежуточные правки |
| оркестратор | манифест, state.js, гейты, деплой-гейт | отчёты пользователю | контексты субагентов |

## Швы для тестов

1. Формат находки аудитора.
2. Валидация фикса — существующий валидатор проекта (index/type, replyMarkup, BFS) + sim-прогон.
3. Гейт деплоя — явное «ок» пользователя перед изменением сервера.
