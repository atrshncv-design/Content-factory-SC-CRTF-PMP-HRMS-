# 32 — Авто-режим (команда «авто», без верификаций)

**What to build:** Команда «авто» для владельца (вкл/выкл): флаг auto_approve в БД. При auto_approve=1 весь цикл без верификаций: сценарий → сразу генерация видео → сразу publish. При 0 (по умолчанию) — верификации из 30/31. Переключение работает и во время цикла: если авто включён — висящие верификации пропускаются автоматически. Статус показывается при вводе команды («⚙️ Авто-режим: вкл/выкл»).

**Done when:** симы команды авто вкл/выкл (флаг в БД), поведение цикла при флаге (пропуск верификаций), валидаторы зелёные.

**Files:** .scratch/client-profiles/fixes/wf-tg-bot.json (команда + ветки), миграция users.auto_approve (v3).
**Status:** done (15.08, верифицировано оркестратором)
