# 27 — wf-creatify-shorts: чистый сценарий, 30 сек, 1 повтор

**What to build:** Шортсы (SH-путь) в wf-creatify-shorts: санитизатор сценария (только чистый текст), контракт промпта на ~30 сек (90–110 слов RU), строгий JSON в теле POST /api/ai_shorts/, перегенерация (1 повтор) при мусоре, ошибка в чат без отправки при 2 неудачах.

**Files:** workflows/wf-creatify-shorts.json (только этот файл)

**Blocked by:** —

**Acceptance:**
- [ ] Exp Parse / санитизатор: служебные строки («Note:», «tags,», «So I should», «Верни ТОЛЬКО» и т.п.), markdown-разметка, JSON-обёртки удаляются; остаётся чистый текст
- [ ] Промпт генерации требует ровно 30 сек: «90–110 слов на русском»; результат вне диапазона (<60 или >150 слов) → флаг «мусор»
- [ ] Тело POST в Creatify — строгий валидный JSON, script = только текст (без пробелов-артефактов)
- [ ] При «мусоре» — 1 повтор генерации; если повтор тоже мусорный — завершение с понятной ошибкой (без вызова /api/ai_shorts/)
- [ ] validate/lint/node --check зелёные; sim: мусор-ответ → перегенерация → чистый текст; 2×мусор → ошибка
- [ ] Деплой на сервер, smoke до точки списания

**Status:** done (15.08 вечер, верифицировано оркестратором)
