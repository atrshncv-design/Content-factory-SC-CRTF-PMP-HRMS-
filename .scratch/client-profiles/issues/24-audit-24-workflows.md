# 24 — Аудит всех 24 не-архивных воркфлоу

**What to build:** Каждый из 24 воркфлоу (live-экспорт в `.scratch/client-profiles/base/`) проверяется: структурная валидность, typeVersion против n8n 2.34.4, свежие исполнения без error, бесплатные smoke-пути. Итог — отчёт-таблица + список найденных проблем для тикета 25.

**Blocked by:** 23 (частично — отчёт учитывает live-наблюдения). Работает субагентами по группам файлов (disjoint).

**Status:** done (15.08, верифицировано оркестратором)

- [ ] Для каждого из 24 воркфлоу: (а) формат JSON (list/1), (б) validate-workflow-json.py (где применимо — BFS от триггера; у wf-tg-bot 0 issues — эталон), (в) lint-workflow-json.py, (г) node --check ВСЕХ jsCode, (д) сверка type:typeVersion с эталоном n8n 2.34.4 (telegram v1.2, switch v3.4, httpRequest 4.5, code 1, webhook 2.x, scheduleTrigger и пр. — сверять с установленными в контейнере), (е) проверка credentials у нод credential-типов (телеграм и пр.), (ж) активность (active) и не-архивность, (з) свежие исполнения: последние N по каждому воркфлоу — status success/error, (и) бесплатные smoke-пути: для воркфлоу с webhook-триггерами — probe (403/404/500), для остальных — что можно без кредитов
- [ ] Отчёт: таблица воркфлоу → PASS/PARTIAL/FAIL по каждому критерию + список проблем (с номером воркфлоу и ноды) → `.scratch/client-profiles/reports/audit-24-15aug.md`
- [ ] НЕ менять файлы воркфлоу (только отчёт)

Примечания: base/ содержит канонический live-экспорт 24 воркфлоу (включая wf-tg-bot — 837 нод финальная). Валидаторы — АБСОЛЮТНЫЕ пути скилла. Кредиты: 0.
