# 23 — Ретест start_cycle + закрытие live-чек-листа волн 1+2

**What to build:** После фикса credentials все бесплатные сценарии волн 1+2 проверяются живьём (пробы + реальные сообщения владельцу, согласовано). Каждый сценарий — зелёный или задокументированный FAIL с причиной.

**Blocked by:** 22 (фикс доставки). Исполняет оркестратор (live-сервер).

**Status:** done (15.08, верифицировано оркестратором)

- [ ] start_cycle (владелец, per-чат профиль 1): гейт проходит → вопрос длительности ДОХОДИТ; ответ «30» → следующий шаг (analyst через hermes, бесплатно) — до точки списания
- [ ] «профиль» → карточка; pf_list → список; pf_switch:1 → переключение; pf_exit → снятие; pf_new → интервью вопрос 1; ответ на Q1 → Q2; «Пропустить» → Q3; «Отмена» → чистка; повторный pf_new с черновиком → «Продолжить с шага N» / «Начать заново»
- [ ] Документ: TXT-файл (реальный, пришлёт пользователь или file_id из проб) → PD-цепочка → /doc-text (TXT без pypdf — работает); PDF → ожидаемый 503 «не настроено» (без pypdf)
- [ ] Фото → /img-text OCR (реальный текст на картинке) — проверить, что Hermes-зрение реально возвращает текст
- [ ] Удаление профиля (pf_del → подтверждение → status='deleted'), операторы (add/remove), вопросы (list/set/reset), платформы (pf_platforms → toggle → сохранение), гейт (фейк-оператор start_cycle → блок)
- [ ] Контекст в промптах: execution_data start_cycle-ветки содержит SC CTX Format с «Клиент: Robotec (…» (из профиля, не хардкод)
- [ ] Каждый тест: exec id, status, ключевые ноды; FAIL → причина + фикс-тикет

Примечания: 0 кредитов; тестовые сообщения слать в чат владельца (согласовано); фейк-пробы — с фейковыми id (тихо).
