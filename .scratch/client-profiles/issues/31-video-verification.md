# 31 — Верификация видео в чате

**What to build:** После готовности видео (webhook/poll) вместо авто-доставки бот шлёт видео с кнопками «✅ Опубликовать» / «🔁 Перегенерировать» / «❌ Отклонить». ✅ → publish на все платформы профиля (как сейчас). 🔁 → новый платный вызов (5 кред/30с) с тем же сценарием. ❌ → отмена цикла (статус отменён). Состояние сессии VIDEO_AWAIT (payload: generation_id + сценарий), ожидание бесконечное. Реализация в wf-creatify-webhook (ветка Telegram stage3) и wf-tg-bot (обработка кнопок).

**Done when:** sim/проверка: видео приходит с кнопками; ✅ → publish; 🔁 → повторный платный POST (не выполняется в тестах — до точки списания); ❌ → отмена. Валидаторы зелёные.

**Files:** workflows/wf-creatify-webhook.json, .scratch/client-profiles/fixes/wf-tg-bot.json.
**Status:** done (15.08, верифицировано оркестратором)
