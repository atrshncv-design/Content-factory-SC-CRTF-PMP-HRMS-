# 25 — Фиксы по результатам аудита

**What to build:** Все проблемы из отчёта аудита (24) устраняются: по воркфлоу, субагентами (disjoint файлы), каждый фикс — validate/lint/node --check зелёные; для wf-tg-bot — те же правила волн 1+2 (паттерны, снапшот connections, esc()).

**Blocked by:** 24 (отчёт). Наполняется после аудита.

**Status:** done (15.08, верифицировано оркестратором)

- [ ] По каждой проблеме из отчёта: фикс в файле воркфлоу (в `.scratch/client-profiles/fixes/<wf>.json` или base-копии для не-основных), валидации зелёные
- [ ] Для wf-tg-bot-фиксов: снапшот connections до мутаций, формат list/1, ensure_ascii=False
- [ ] Сводный список: воркфлоу → что исправлено → статус
- [ ] НЕ трогать платные вызовы (только структурные/runtime-фиксы); кредиты 0

Примечания: тикет наполняется фактическими пунктами после выхода отчёта 24; фиксы применяются на сервер в 26 после зелёных валидаций.
