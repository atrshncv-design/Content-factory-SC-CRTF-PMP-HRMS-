# РЕВЬЮ 3 — Системный слой контент-завода (06-system-cluster)

**Дата:** 14.08.2026 · **Метод:** статическое локальное ревью, 0 сетевых вызовов (правило 0 кредитов)
**Объём:** SSRF-защита wf-onboard, wf-tg-alerts, zz-test-sqlite, скрипты (register-tg-commands.sh, phase2-enable.sh),
секрет-скан (дерево + полная git-история + dangling-объекты), docker-compose.yml, DEPLOYMENT.md (548 строк, сверка с фактами репо),
спеки specs/ (webhook-контракты), infra/ (миграции, db-bridge).
**Прецедент:** docs/CODE-REVIEW-2026-08-13.md (ревью 2; К1 — историческая утечка, вычищена filter-repo 13.08, коммит cc3b65d).

---

## 1. ТАБЛИЦА НАХОДОК

| # | область | severity | файл:строка | факт | влияние | доказательство |
|---|---------|----------|-------------|------|---------|----------------|
| С1 | SSRF-защита wf-onboard | 🔴 | workflows/wf-onboard.json:39 (нода «SSRF check») | **Обход приватных диапазонов через IPv6/IPv4-mapped литералы.** Парсинг hostname: `hostport.split(':')[0]` для `[::1]` даёт `[`, после `.replace(/^\[|\]$/g,'')` — пустая строка → `blocked=false`. `http://[::1]/`, `http://[::ffff:10.0.0.1]/`, `http://[::ffff:192.168.1.1]/` проходят фильтр и уходят в HTTP Request. Проверка `hostname === '::1'` недостижима (баг скобок) | SSRF к локальным службам из контейнера n8n (n8n:5678, db-bridge:8787 на 0.0.0.0), обход требований спеки 08:214-216 («запретить 10/8, 172.16/12, 192.168/16, 127/8, ::1») | jsCode ноды «SSRF check»: `let hostport = m[1]; let hostname = hostport.split(':')[0]...replace(/^\[|\]$/g,'')`; `[::1]` → hostname='' → `blocked` остаётся `false`; спека specs/08-onboarding.md:214-216 требует блок ::1; ревью 2 (В5: «не покрыты IPv6») — подтверждено и конкретизировано |
| С2 | error-ветка wf-onboard | 🔴 | workflows/wf-onboard.json:39,44 (SSRF check/HTTP Request) | **Нет error-ветки и нет error-воркфлоу.** Throw в Code-ноде («URL is required…», «SSRF blocked…») обрывает исполнение; responseMode=responseNode → клиент получает сырой 500 с текстом ошибки. Ни в одном из 24 воркфлоу репо нет onError/errorWorkflow/Error Trigger | Неконсистентный контракт ответа при невалидном url/SSRF; утечка деталей проверки в текст ответа; нет алерта оператору о подозрительных запросах | grep onError/errorWorkflow по workflows/*.json — 0 попаданий; ревью 2 В5 («нет error-ветки») — не исправлено |
| С3 | DEPLOYMENT.md правдивость | 🔴 | DEPLOYMENT.md:32 | **Заявлен воркфлоу wf-credit-check с live-тестом** `✅ 200 {ok:true, balance:497} (13.08, live)` — файла воркфлоу НЕТ в репо (0 файлов с «credit» в имени) | Документ вводит в заблуждение: тестировщик/заказчик поверит в существование и работу эндпоинта `/webhook/factory/credit-check` | ls workflows/ — нет wf-credit-check.json; ревью 2 (§4: «Воркфлоу НЕТ (0 с "credit" в имени) ❌ строка DEPLOYMENT.md:32 — артефакт») — **не исправлено** |
| С4 | DEPLOYMENT.md правдивость | 🔴 | DEPLOYMENT.md:329 vs :360 | **Перепутан id wf-creator-profile:** :329 утверждает `id ...015`, фактический id воркфлоу — `...016` (...015 = wf-creators-search). При этом :360 того же документа корректно пишет `id ...016` — внутреннее противоречие | Невозможно однозначно сопоставить документ с реальностью (какой id у profile?) | workflows/wf-creator-profile.json: `"id": "20000000-...-000000000016"`; workflows/wf-creators-search.json: `...015`; DEPLOYMENT.md:329 (полная строка): «JSON: ~/factory/wf-creator-profile.json (id `...015`, 14 нод, active=1…)»; :360: «id `...016`»; ревью 2 (§4) — **не исправлено** |
| С5 | репо ↔ заявленное состояние | 🟠 | workflows/*.json (8 файлов) | **Рассинхрон active-флагов:** 8 из 24 воркфлоу в репо `active=false`: wf-creators-search (...015), wf-creator-profile (...016), wf-creator-content (...014), wf-transcripts-comments (...018), wf-creatify-asset (...021), wf-creatify-adclone (...022), wf-creatify-product (...024), wf-creatify-banner (...025). DEPLOYMENT:23 «Воркфлоу n8n (все активны)» + точечные claims `active=1` для ...021/:442, ...022/:453, ...024/:479, ...025/:491 | Экспорты репо ≠ документ ≠ (по ревью 2) сервер (24/24 active). Канонический источник неопределён: повторный импорт из репо деактивирует воркфлоу (импорт снимает активность — DEPLOYMENT:85) | Извлечение id/active из всех 24 JSON: 16 active / 8 inactive; файлы от 13.08 19:07; DEPLOYMENT:442/453/479/491 заявляют active=1 для файлов с active=false |
| С6 | register-tg-commands.sh | 🟠 | register-tg-commands.sh:21,65-77 | **Скрипт устарел и опасен при повторном запуске:** регистрирует 28 команд (PAYLOAD=tg-commands-25.json, verify=28), но §27 DEPLOYMENT фиксирует деплой **31 команды** (register-tg-commands-31.sh, getMyCommands=31) — файла 31-версии скрипта в репо НЕТ, tg-commands-31.json untracked. Запуск репо-скрипта на сервере затрёт меню UX-2 (31→28, удалит menu/instruction/url2video), verify (28≠31) будет вечно фейлиться | Потеря меню UX-2 и инструкции при переустановке; 5 попыток ре-регистрации 28 команд поверх 31 | tg-commands-25.json: 28 команд; tg-commands-31.json: 31 команда (+menu, instruction, url2video); git status: `?? tg-commands-31.json` (не закоммичен); DEPLOYMENT.md §27 (рабочее дерево, +18 строк): «setMyCommands: tg-commands-31.json (31 команда…)» |
| С7 | docker-compose.yml | 🟠 | docker-compose.yml:9-35,39-69 | **Compose устарел vs live-архитектура** (ревью 2 В14, не исправлено): нет сервисов db-bridge/cloudflared/hermes-bridge; нет `extra_hosts` (DEPLOYMENT:87 — 149.154.167.220/.99; :252 — host.docker.internal); нет `N8N_BLOCK_ENV_ACCESS_IN_NODE=false` (DEPLOYMENT:83 — без него `$env` в выражениях не работает); `n8nio/n8n:latest` без пина версии; порты 5678/8642 опубликованы на 0.0.0.0; `N8N_SECURE_COOKIE=false` помечен «на проде пересмотреть» | Повторный деплой из репо ≠ прод-конфигурация: сломаются $env-выражения, TG-фолбэк-IP, доступ к db-bridge; n8n UI/API и Hermes-API публично доступны при открытом firewall | grep: BLOCK_ENV/extra_hosts в compose — отсутствуют (упоминания только в DEPLOYMENT.md:83,87,252 и docs/*); ревью 2 В14 |
| С8 | infra/db-bridge | 🟠 | infra/db-bridge/server.js:26,107-116,24 | **Fail-open авторизация:** `const TOKEN = process.env.FACTORY_DB_BRIDGE_TOKEN || ''` + `if (TOKEN) {...}` — при пустом токене проверка пропускается, /query доступен без auth с полным SQL (SELECT/INSERT/UPDATE/DELETE/PRAGMA/WITH). Плюс HOST по умолчанию `0.0.0.0`, хотя шапка (стр.4) обещает «слушает docker0 (172.17.0.1:8787)»; документированный `POST /exec` (шапка стр.10) не реализован — 404 | При деплое без токена — полный доступ к factory.db у любого, кто достучится до порта; расхождение доки и кода | server.js:26,107-116,102-106 (только /query, /exec→404); ревью 2 В13 («fail-open… на проде токен задан — риск снят, но код опасен») — **не исправлено**; контраст: hermes-bridge/server.py:44 — `if not TOKEN or not hmac.compare_digest(...)` → 401 fail-closed |
| С9 | wf-tg-alerts | 🟠 | workflows/wf-tg-alerts.json:31-32 | **Открытый ретранслятор:** webhook `factory/tg-alert` без auth, без валидации, без лимитов — `chat_id`/`text` берутся из тела (`{{ $json.body.chat_id }}`, `{{ $json.body.text }}`); нет parse_mode → Markdown/HTML-инъекции исключены, но любой, кто достучится до n8n (Caddy проксирует /webhook/*), может слать сообщения от бота в ЛЮБЫЕ чаты, где бот состоит; отсутствующие поля → ошибка sendMessage | Спам-релей/фишинг от имени бота; нестабильный контракт (нет 400-валидации) | wf-tg-alerts.json: Webhook→Telegram напрямую (2 ноды, 0 валидации); TICKETS.md:378 — контракт пути `/webhook/factory/tg-alert` совпадает ✓; DEPLOYMENT:27 документирует {chat_id, text} |
| С10 | спеки webhook (creatify) | 🟠 | workflows/wf-creatify-webhook.json:22 vs specs/04-generation.md:217-218, TICKETS.md:349 | **Path-token callback СТАТИЧЕН и публичен:** фактический путь `factory/creatify/6d8f2a41c9e7b3d5f0a1c4e8` — константа в репо; спека 04 требует «случайный токен в WEBHOOK_URL», TICKETS-349: `/webhook/creatify/<path-token>`. Старые спеки (00:125,146; 03:181; 04:87,181) ожидают путь `/webhook/creatify` без префикса factory/ и без токена. Проверки подписи callback нет (ревью 2 В8) | Знание пути из репо + отсутствие подписи → спуфинг done/failed-колбэков (пометка генераций done/failed чужим POST'ом); три разных пути в трёх поколениях спек | wf-creatify-webhook.json: параметр path = `factory/creatify/6d8f2a41c9e7b3d5f0a1c4e8` (статический UUID); specs/04-generation.md:217-218; TICKETS.md:349; specs/00-architecture.md:125 «production/webhook/creatify» |
| С11 | zz-test-sqlite | 🟠 | workflows/zz-test-sqlite.json:31,13 | **Тестовый эндпоинт активен на прод-пути:** `factory/_test` (active=true) читает `SELECT key, value FROM settings LIMIT 3` из `/var/data/factory.db` и эхо-возвращает всё тело запроса — без auth, публично через Caddy | Раскрытие внутренностей БД (в т.ч. settings, где на сервере живут балансы/состояния) и эхо произвольных тел; лишняя публичная поверхность | zz-test-sqlite.json: Webhook(factory/_test)→Code(node:sqlite)→Respond; DEPLOYMENT:39 «служебный»; локальная data/factory.db: settings без секретов (12 ключей: mode, лимиты, client_name…) |
| С12 | Caddy / n8n UI | 🟠 | infra/Caddyfile:8-14, docker-compose.yml:16 | **Caddy публикует ВЕСЬ n8n UI/API на публичном nip.io** (catch-all `handle { reverse_proxy n8n:5678 }`, домен 83.166.233.95.nip.io), а `N8N_BASIC_AUTH` в compose НЕТ; `/media/*` раздаётся без авторизации | Публичный доступ к UI/API n8n (смена воркфлоу, креды) при открытом 80/443; медиа-файлы клиентов публичны. Частично смягчено firewall VK Cloud (ревью 2: «HTTP 000 снаружи» для 5678/8642/8787) | infra/Caddyfile: handle catch-all → n8n:5678; docker-compose.yml: N8N_BASIC_AUTH отсутствует (grep — 0); ревью 2 §5 🟠 |
| С13 | миграции БД | 🟡 | infra/db/migrate.sh:28-38 | **Локальная БД отстала:** data/factory.db — schema_version=[1], таблицы sessions НЕТ (002_sessions.sql не применена); migrate.sh корректен по идемпотентности (schema_version пишут сами SQL: 001:176, 002:23), но миграции не оборачиваются в транзакцию, а `sqlite3 ... || echo 0` при отсутствующей таблице маскирует реальные ошибки | Локальная среда не отражает схему прода (на сервере sessions есть — DEPLOYMENT:248); риск частичного применения при сбое | sqlite3 data/factory.db: schema_version=[1], tables без sessions; 002_sessions.sql создаёт sessions + INSERT schema_version=2 |
| С14 | phase2-enable.sh | 🟡 | phase2-enable.sh:8-9,203 | **Ключи передаются аргументами CLI** → видны в ps/history (одноразовый запуск — приемлемо, но хрупко); строка 203 хардкодит протухающий trycloudflare-URL (туннель меняется при рестарте). `--dry-run` (plan без записи), маскирование `v[:4]+"***"+v[-4:]`, chmod 600, y/N-подтверждение, пропуск OTHER-ключей — всё на месте ✓ | Утечка ключей в shell-истории сервера; устаревшая инструкция в выводе | phase2-enable.sh:79-140 (mode=plan|apply), 146-194 (dry-run, chmod 600), 203 |
| С15 | DEPLOYMENT.md:90 | 🟡 | DEPLOYMENT.md:90 | **«Корневые id воркфлоу — фиксированные (20000000-...-001..012)»** — фактически id доходят до `...025`, а wf-onboard имеет случайный UUID `0dbb4ea9-0cf0-4672-b1a0-973dfe8cb743` | Утверждение не отражает текущий набор id; вводит в заблуждение субагентов/разработчиков | Извлечение id из 24 JSON: ...001..025; wf-onboard.json:4 `"id": "0dbb4ea9-..."` |
| С16 | register-tg-commands.sh verify | 🟡 | register-tg-commands.sh:66-77,86-89 | **verify_ours проверяет только default-scope** (getMyCommands без параметра scope), хотя set_commands вызывается для 3 scopes (default/all_private_chats/all_group_chats); scope `all_chat_administrators` не покрыт; хардкод IP `149.154.167.220` в --resolve (может устареть); exit 0 всегда — ошибки маскируются | Сбой регистрации в private/group-scope не будет обнаружен; при ротации IP Telegram скрипт молча не сработает | register-tg-commands.sh:46-63 (3 scopes), 68 (getMyCommands без scope), 25 (TG_IP), 100-105 (exit 0) |
| С17 | доки vs доки | 🟡 | register-tg-commands.sh:12-13 vs DEPLOYMENT.md:253-254 | **Противоречие:** шапка скрипта заявляет «Автозапуск: ExecStartPost в hermes.service», DEPLOYMENT:253-254 — «Hermes-gateway ОСТАНОВЛЕН… register-tg-commands.sh убран из ExecStartPost» | Непонятно, исполняется ли скрипт на сервере (если да — конфликт с 31-командным меню, см. С6) | register-tg-commands.sh:12-13; DEPLOYMENT.md:253-254 |
| С18 | DEPLOYMENT.md структура | 🟡 | DEPLOYMENT.md:327-392,452-491 | **Дубли нумерации разделов:** два блока «## 16. SC-2» (:327 LIVE и :359 DONE), два «## 17. SC-4» (:367, :391), два «## 22» (:452, :470), два «## 23» (:478, :489) | Путаница при цитировании (какой §16?); риск пропуска одного из блоков | DEPLOYMENT.md:327,352,359,367,391,452,470,478,489 |

---

## 2. СЕКРЕТ-СКАН (значения НЕ выводились)

| Канал | Паттерны | Результат |
|-------|----------|-----------|
| Git-история (rev-list --all, 12 коммитов, ВСЕ файлы включая .md) | `sk-w[A-Za-z0-9]{30,}`, `FactoryOwner2026`, TG-токены `\d{8,10}:[A-Za-z0-9_-]{35}`, `(api_key|secret|token|password)[=:]` + 32-64 симв. | **0 попаданий** — вычистка filter-repo (13.08, cc3b65d) подтверждена |
| Dangling-объекты (git fsck --lost-found) | те же | 0 dangling-коммитов, 0 попаданий |
| Рабочее дерево (исключая .git/.scratch/data) | те же | **1 кандидат:** `.env:9` — токен Telegram-бота, формат реального (46 симв., не заглушка), файл gitignored, в git никогда не попадал. **1 ложное срабатывание:** infra/db-bridge/server.js:26 — имя переменной `FACTORY_DB_BRIDGE_TOKEN` (35 симв.) |
| Статические артефакты | — | infra/n8n-import/creds.json: только `PLACEHOLDER_UNTIL_TOMORROW` / `__TELEGRAM_TOKEN__` ✓; specs/11-amendments.md:204 — `OPENCODE_ZEN_API_KEY=***` замаскировано ✓; settings в data/factory.db — без секретов (имена ключей: mode, лимиты, client_name…) ✓ |

**Вывод:** реальных секретов в git-истории и репо — **0**. Локальный `.env:9` (TG-токен, gitignored) — вне репо; значение скрыто; ротация не требуется для репо, но токен живой на dev-машине — держать вне любых коммитов. Остаётся открытой ротация на СЕРВЕРЕ (пароль n8n owner + OPENCODE_ZEN_API_KEY, docs/CODE-REVIEW-2026-08-13.md §5) — за пользователем.

## 3. ПРОВЕРЕНО И РАБОТАЕТ

- **SSRF-фильтр (IPv4-часть):** блокирует 10/8, 172.16/12, 192.168/16, 127/8, 169.254/16, 0.0.0.0, `localhost`, `.local`, `.internal`, `.localhost`; `followRedirects:false` (защита от redirect-обхода); валидация схемы http(s) и обязательности `body.url` — соответствует спеке 08:214-216 (кроме ::1, см. С1).
- **Секретная гигиена:** история чиста (0/0/0), creds.json — плейсхолдеры, .gitignore покрывает .env/данные/ключи, hermes-bridge fail-closed (server.py:44 → 401).
- **Webhook-пути спеки ↔ воркфлоу:** актуальные спеки (TICKETS-EXPANSION.md:159,183,206,229,253,254,290,332,349,366,383; 11-amendments.md:92-96; TICKETS.md:378) — **18/18 совпадают** с фактическими путями воркфлоу. Расхождения только в legacy-спеках 00/03/04 по creatify-callback (см. С10).
- **phase2-enable.sh:** --dry-run реально ничего не пишет (mode=plan), маскирование первых4+последних4 символов, `chmod 600` после записи, y/N-подтверждение, чужие значения (OTHER) не перезаписываются.
- **wf-tg-alerts:** контракт пути и тела {chat_id, text} совпадает со спекой; parse_mode не задан → Markdown/HTML-инъекций нет.
- **db-bridge:** single-statement guard, allowlist префиксов (SELECT/INSERT/UPDATE/DELETE/REPLACE/PRAGMA/WITH), timingSafeEqual, лимит тела 256KB.
- **migrate.sh:** идемпотентен (schema_version пишут сами SQL-файлы 001:176, 002:23), порядок по номеру файла, set -euo pipefail.
- **Числа нод:** wf-tg-bot = 404 (соответствует §27 «278 → 404»), wf-creatify-webhook = 25 («21 → 25»), wf-creatify-shorts = 17 («12 → 17») ✓; id ...017/...020/...019/...021/...022/...023/...024/...025 в §17-23 DEPLOYMENT соответствуют файлам ✓ (кроме ...015/...016 — С4).
- **tg-commands-25.json = 28 команд** — внутренне консистентен со старым verify-набором (28 имён) ✓ (устарел относительно 31 — С6).

## 4. ВЕРДИКТ СИСТЕМНОГО СЛОЯ

**🟠 УСЛОВНО ПРОХОДИТ С ОГОВОРКАМИ — не готов к приёмке без фиксов.**

Сильная сторона слоя — секретная гигиена (история чиста, плейсхолдеры везде) и согласованность актуальных спек с путями воркфлоу. Но:
1. **SSRF-фильтр обходится через IPv6-литералы** (С1) — прямое нарушение спеки 08 и обещания DEPLOYMENT:28 «SSRF 127.0.0.1 отклонён»; вместе с отсутствием error-ветки (С2) делает wf-onboard главной дырой слоя. **Фикс:** парсить hostname через `new URL()`-эквивалент (в Code-ноде URL недоступен — DEPLOYMENT:84) или корректный разбор скобок + блок `::/0` и `::ffff:0:0/96`; добавить Error Trigger/error-воркфлоу с JSON-ответом.
2. **DEPLOYMENT.md содержит ложь/противоречия** (С3 — несуществующий wf-credit-check с live-тестом; С4 — id ...015 vs ...016 в одном документе) — оба зафиксированы ревью 2 и **не исправлены**.
3. **Репо разошёлся с продом:** 8 воркфлоу inactive в файлах (С5), compose без db-bridge/extra_hosts/BLOCK_ENV (С7), скрипт регистрации 28-командный против 31 на сервере (С6) — повторный деплой из репо деградирует прод.
4. **db-bridge fail-open** (С8) — код опасен, спасён только тем, что на проде задан токен.

Порядок: С1+С2 (безопасность wf-onboard) → С3/С4 (правдивость документа) → С5-С7 (синхронизация репо с продом) → С8-С12 (периметр) → мелочи С13-С18.

*Ревью статическое, 0 кредитов потрачено. Все 🔴 перепроверены по файлам репо и git-истории.*
