# D1 — Правдивость документации (Y14/Y15): исправления

Дата: 14.08.2026. Агент: D1 (фикс-волна по docs/CODE-REVIEW-2026-08-14.md).
Правило: 0 кредитов — только локальные файлы. Секреты не выводятся.

## Внесённые правки

| Файл:строка | Было | Стало | Почему |
|---|---|---|---|
| DEPLOYMENT.md:20 | `Воркфлоу n8n (11 шт, все активны)` | `Воркфлоу n8n (все активны; полный перечень с id — п. 5.12)` | Счётчик «11 шт» устарел (в таблице 13 строк, реально воркфлоу 24: ...001–...025 + wf-onboard); отсылка к авторитетному списку id (п. 5.12) вместо ложного числа |
| DEPLOYMENT.md:32 | `\| wf-credit-check \| POST \`/webhook/factory/credit-check\` {} \| ✅ 200 {ok:true, balance:497} (13.08, live) \|` | `\| wf-credit-check — ❌ НЕ существует \| — \| Баланс creatify проверяется бесплатным GET \`/api/remaining_credits/\` (воркфлоу wf-credit-check НЕ существует — удалён из архитектуры 13.08, не создавать) \|` | Воркфлоу wf-credit-check НЕ существует ни в репо, ни на сервере (проверено оркестратором 14.08) — строка заявляла несуществующий воркфлоу с выдуманным live-тестом; баланс creatify проверяется бесплатным GET /api/remaining_credits/ |
| DEPLOYMENT.md:90 | `фиксированные (20000000-...-001..012)` | `фиксированные (20000000-...-001..025 + wf-onboard = UUID 0dbb4ea9-0cf0-4672-b1a0-973dfe8cb743)` | Диапазон ...001..012 устарел: реальные id воркфлоу — ...001..025 (проверено по workflows/*.json в репо), wf-onboard имеет UUID 0dbb4ea9-0cf0-4672-b1a0-973dfe8cb743 |
| DEPLOYMENT.md:144 | `Slash-команды (15, зарегистрированы в Telegram через setMyCommands)` | `Slash-команды (ядро — 15; всего зарегистрировано 31 через setMyCommands, см. §27)` | Утверждение «зарегистрированы 15» противоречит §27 (строка 546): setMyCommands = tg-commands-31.json, 31 команда |
| DEPLOYMENT.md:329 | id `20000000-0000-4000-8000-000000000015` (wf-creator-profile) | id `20000000-0000-4000-8000-000000000016` | ...015 = wf-creators-search, ...016 = wf-creator-profile (проверено: workflows/wf-creator-profile.json и wf-creators-search.json, сервер подтверждён оркестратором). После правки строка 329 согласована со строкой 360 (`id ...016`) |

## Проверено — правки НЕ потребовались

| Файл | Проверка | Результат |
|---|---|---|
| register-tg-commands-31.sh | PAYLOAD, verify, scope | ✅ PAYLOAD=`$FACTORY_DIR/tg-commands-31.json` (:12); verify: `total=31` + want-set из 31 имён (:58, :62); все 3 scope: default / all_private_chats / all_group_chats (:70–72). Совпадает с эталоном сервера, правок нет |
| tg-commands-31.json | ровно 31 команда, имена как в Switch cmd | ✅ 31 команда, дублей нет. Все 31 имя присутствуют в rightValues ноды `Switch cmd` (workflows/wf-tg-bot.json). Правок нет |
| DEPLOYMENT.md — прочие упоминания id воркфлоу | grep `...0[0-9][0-9]` + сверка с фактами | ✅ Согласованы все: zz-test ...001 (только упоминание в строке 90 как часть диапазона; воркфлоу-таблица строку 39 не имеет id), wf-tg-alerts ...00a, analytics ...00b, creatify-link ...00c, submit ...00d, webhook ...00e, poll ...00f, publish ...010 (:399), publish-status ...011, sync-accounts ...012, tg-bot ...013 (:257, :268), creator-content ...014 (:304), creators-search ...015 (без id в тексте), creator-profile ...016 (:329 исправлен, :360), audience ...017 (:369, :392), transcripts-comments ...018 (:416), creatify-avatar ...019 (:434), creatify-text ...020 (:423), asset ...021 (:442), adclone ...022 (:453), shorts ...023 (:471), product ...024 (:479), banner ...025 (:491). Упоминания «кред ...001» — id credential (10000000-...-000000000001), не воркфлоу — корректны |

## Замечания (вне скоупа, НЕ правил)

- `Switch cmd` содержит 33 правила: 31 публичная команда + 2 внутренние (`hint`, `dur` — хинт-кнопки/выбор длительности, в меню НЕ регистрируются). Это by design, tg-commands-31.json корректен.
- Строки 505–506: «HTTP credit-check» — это имя НОДЫ внутри wf-creatify-banner (GET /api/remaining_credits/), а не воркфлоу — не противоречит исправленной строке 32.
- Дублирование номеров разделов «## 15/## 16» (строки 327/352/359) и историческая секция §26 (28 команд, tg-commands-25.json) — pre-existing, не трогал (историческая запись на момент 13.08 верна, актуальное состояние описано в §27).
