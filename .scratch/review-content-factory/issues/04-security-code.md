# REVIEW-04 — Безопасность + код вне n8n + сверка DEPLOYMENT.md (локальная часть)

**Status:** done (13.08.2026, см. docs/CODE-REVIEW-2026-08-13.md)
**Blocked by:** —

## Задача
Аудит безопасности и кода вне воркфлоу в репо
`/Users/aleksandrtrisenkov/Desktop/PROGRAMMING/РАБОЧИЕ ПРОЕКТЫ/КОНТЕНТ-ЗАВОД-API-MVP/`.
ЖИВАЯ сверка с сервером (SSH) — НЕ твоя задача (её делает оркестратор отдельно).
Ты работаешь ТОЛЬКО с файлами репо.

## Чек-лист
### A. git history / секреты
1. `git log --all --oneline` — какие коммиты есть.
2. Скан всего дерева репо (git grep по HEAD и по всему history) на утечки:
   строгие паттерны секретов (длинные sk-*, Bearer-токены, X-API-KEY со значением,
   n8n owner-пароль, ключи в workflows/*.json). Маски-паттерны (`sk-PLA..._KEY`,
   `$SCRAP...EY`, `PLACEHOLDER_UNTIL_TOMORROW`, `[скрыто]`) — НЕ секреты.
   Известный факт: утечка пароля n8n UI в docs/prompts/*.md замаскирована коммитом 7cd88fd —
   проверить, что маскирование полное, и поискать ДРУГИЕ утечки.
3. .gitignore: .env, data/, *.db*, media/, *.pem — игнорируются? Секреты не в репо?

### B. hermes-bridge/server.py
1. ALLOWED_SKILLS whitelist — что в списке, соответствует ли скиллам в ~/.hermes/skills (см. hermes/skills/ в репо).
2. Проверка X-BRIDGE-TOKEN — сравнивается безопасно (constant-time не обязательно, но не пустая)?
3. Субпроцесс-вызов: shell=True? инъекция аргументов (skill/prompt) возможна?
4. Обработка ошибок, таймауты, --reasoning none для caption-adapter.
5. Слушает 127.0.0.1:8642 или 0.0.0.0? (файл в репо, но проверить константы).

### C. Скрипты
1. `phase2-enable.sh`: маскирование в dry-run, подтверждение [y/N], права 600, не логирует ключи.
2. `register-tg-commands.sh`: не содержит секретов, exit 0 всегда (питфолл restart-loop).
3. `infra/`, `hermes/` — что там, нет ли секретов.
4. `docker-compose.yml`: extra_hosts для api.telegram.org, N8N_BLOCK_ENV_ACCESS_IN_NODE=false,
   db-bridge порт 8787 наружу — оценка риска.

### D. Сверка документации (локальная часть)
1. `DEPLOYMENT.md`: заявленные id воркфлоу/статусы — сопоставить с workflows/*.json
   (id в JSON). Найти расхождения «в доке сказано X, в JSON другое».
2. `tg-commands-25.json` vs tg-commands-15.json vs количество веток в wf-tg-bot.json
   (если доступно) — команды зарегистрированы, но веток нет?
3. `specs/TICKETS-EXPANSION.md`: статусы DONE/BLOCKED — заявлено vs следы в репо.

## Формат отчёта (markdown)
- Таблица находок: | # | Область | Severity (🔴/🟠/🟡/🟢) | path:line | Описание | Рекомендация |
- Раздел «Секреты»: подтверждено чисто / найдены утечки (маскированно).
- Раздел «Расхождения DEPLOYMENT vs файлы»: таблица.
- Раздел «Непроверенное» (требует живого сервера — оркестратор сделает отдельно).

## Ограничения
- ТОЛЬКО чтение файлов репо + git-команды (git log/grep/show). Никаких сетевых вызовов, SSH, API.
- Секреты не выводить; при находке — маскированный фрагмент (первые 4 + последние 4 символа).
- Код НЕ менять. Отчёт — в ответе (не файл).
- Язык отчёта: русский.
